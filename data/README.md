Data files are not included in this repo.
Tables were populated from a synthetic transaction monitoring dataset (CSV).
You can reproduce results by importing CSVs into PostgreSQL and running `sql/02_case_assignment_rolling_30d.sql` + `sql/03_views.sql`.
